- @goals.each do |goal|
  .well
    .goal_header
      %h3= goal.title
      %p= goal.description
    = link_to "Update Goal", edit_goal_path(goal)
    = link_to "Delete Goal", goal_path(goal), method: :delete